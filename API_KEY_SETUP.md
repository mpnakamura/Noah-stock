# APIキー設定ガイド

## エラー: "Failed to generate mindmap"

このエラーは、Anthropic API Keyが正しく設定されていないことが原因です。

---

## 解決方法

### 1. API Keyを取得

1. [Anthropic Console](https://console.anthropic.com/) にアクセス
2. アカウントを作成またはログイン
3. 「API Keys」ページで新しいキーを作成
4. キーをコピー（`sk-ant-...` で始まる文字列）

### 2. `.env.local` ファイルを作成

プロジェクトのルートディレクトリで以下を実行：

```bash
# .env.example をコピー
cp .env.example .env.local
```

### 3. APIキーを設定

`.env.local` ファイルを開き、あなたのAPIキーを設定：

```bash
# .env.local
ANTHROPIC_API_KEY=sk-ant-あなたのAPIキーをここに貼り付け
```

**重要**:
- `your_api_key_here` を実際のAPIキーに置き換えてください
- `sk-ant-` で始まる文字列全体をコピーしてください

### 4. 開発サーバーを再起動

```bash
# 既存のサーバーを停止（Ctrl+C）

# 再起動
npm run dev
```

---

## 確認方法

### ステップ1: ファイルが存在するか確認

```bash
ls -la .env.local
```

表示されればOK。表示されなければステップ2に戻る。

### ステップ2: APIキーが設定されているか確認

```bash
cat .env.local
```

以下のように表示されることを確認：

```
ANTHROPIC_API_KEY=sk-ant-api03-... (実際のキー)
```

### ステップ3: サーバーログを確認

開発サーバーのターミナルで以下のようなエラーがないことを確認：

```
✓ Ready in 2.3s
○ Local: http://localhost:3000
```

エラーがある場合：

```
Error: ANTHROPIC_API_KEY is not configured
```

→ ステップ2に戻り、APIキーが正しく設定されているか確認

---

## トラブルシューティング

### エラー1: "ANTHROPIC_API_KEY is not configured"

**原因**: 環境変数が読み込まれていない

**解決策**:
1. `.env.local` ファイルが存在するか確認
2. ファイル名が正しいか確認（`.env` ではなく `.env.local`）
3. 開発サーバーを再起動

### エラー2: "Authentication error" / 401 Unauthorized

**原因**: APIキーが無効または間違っている

**解決策**:
1. Anthropic Consoleでキーが有効か確認
2. キーをコピー&ペーストし直す
3. スペースや改行が入っていないか確認

### エラー3: "Rate limit exceeded"

**原因**: APIの使用制限に達している

**解決策**:
1. Anthropic Consoleでクレジットがあるか確認
2. 数分待ってから再試行

### エラー4: マインドマップが生成されない（ローディングのまま）

**原因**: ネットワークエラーまたはAPIタイムアウト

**解決策**:
1. インターネット接続を確認
2. ブラウザのコンソール（F12）でエラーを確認
3. ページをリロードして再試行

---

## セキュリティ注意事項

⚠️ **絶対にやらないこと**:

- `.env.local` をGitにコミットしない（すでに `.gitignore` で除外済み）
- APIキーを他人と共有しない
- APIキーを公開リポジトリにプッシュしない
- APIキーをスクリーンショットで共有しない

✅ **推奨事項**:

- APIキーは個人用として管理
- 不要になったキーはAnthropicコンソールで削除
- 定期的にキーをローテーション

---

## 動作確認

正しく設定できたら、以下を試してください：

1. ブラウザで http://localhost:3000 を開く
2. テキストエリアに「ECサイトにユーザー認証機能を追加」と入力
3. 「マインドマップを生成」をクリック
4. 5-10秒でマインドマップが表示される

成功すれば、APIキーの設定は完了です！🎉

---

## サポート

それでも解決しない場合：

1. ブラウザのコンソール（F12 → Console）でエラーを確認
2. 開発サーバーのターミナルでエラーを確認
3. GitHubのIssuesで質問
